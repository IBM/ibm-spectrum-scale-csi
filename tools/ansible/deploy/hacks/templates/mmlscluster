Value Filldown ID (\d+)
Value Filldown Name ([^\s]+)
Value Node ([^\s]+)
Value Designation ([\w-]+)

Start
  ^\s+GPFS cluster name:\s+${Name}
  ^\s+GPFS cluster id:\s+${ID}     
  ^\s+\d+\s+${Node}\s+[^\s]+\s+[^\s]+\s+${Designation} -> Next.Record

