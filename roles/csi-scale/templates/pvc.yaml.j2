apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: 'scale-pvc'
    namespace: '{{ meta.namespace }}'
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-spectrum-scale
